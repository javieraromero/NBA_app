import { ScheduleSearchPipe } from './schedule-search.pipe';

describe('ScheduleSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new ScheduleSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
