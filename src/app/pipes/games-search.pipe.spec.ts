import { GamesSearchPipe } from './games-search.pipe';

describe('GamesSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new GamesSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
