import { PlayersSearchPipe } from './players-search.pipe';

describe('PlayersSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new PlayersSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
