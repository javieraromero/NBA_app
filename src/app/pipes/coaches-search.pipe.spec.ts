import { CoachesSearchPipe } from './coaches-search.pipe';

describe('CoachesSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new CoachesSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
