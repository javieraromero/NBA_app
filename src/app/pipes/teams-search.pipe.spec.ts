import { TeamsSearchPipe } from './teams-search.pipe';

describe('TeamsSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new TeamsSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
